# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# QuickBooks Integration
VITE_QB_CLIENT_ID=your_quickbooks_client_id
VITE_QB_CLIENT_SECRET=your_quickbooks_client_secret

# Optional: Keep for backward compatibility if needed
VITE_API_URL=http://localhost:8000

# Vercel Deployment Notes:
# 1. Set these variables in Vercel Dashboard > Settings > Environment Variables
# 2. For production, use your actual Supabase and QuickBooks credentials
# 3. QuickBooks redirect URI should be: https://your-app.vercel.app/qb-callback
# 4. Never commit actual secrets to Git - use Vercel environment variables

# Server-only (set in Vercel, not exposed to client)
# Used for verifying QuickBooks webhooks
QB_VERIFIER_TOKEN=your_quickbooks_verifier_token
